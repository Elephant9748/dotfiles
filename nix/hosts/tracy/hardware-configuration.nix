
# Do not modify this file!  It was generated by ‘nixos-generate-config’
# and may be overwritten by future invocations.  Please make changes
# to /etc/nixos/configuration.nix instead.
{ config, lib, pkgs, vars, modulesPath, ... }:

{
  imports =
    [ (modulesPath + "/profiles/qemu-guest.nix")
    ];

  boot.initrd.availableKernelModules = [ "ahci" "xhci_pci" "virtio_pci" "sr_mod" "virtio_blk" ];
  boot.initrd.kernelModules = [ "dm-snapshot" ];
  boot.kernelModules = [ "kvm-intel" ];
  boot.extraModulePackages = [ ];

  # Use the systemd-boot EFI boot loader.
  boot = {
  	loader = {
		systemd-boot.enable = true;
		systemd-boot.configurationLimit = 5;
		efi.canTouchEfiVariables = true;
	};
	initrd.luks.devices.cryptroot = {
		device = "/dev/disk/by-uuid/577039f5-5c11-4c84-8a5c-70f964d0e129";
		allowDiscards = true;
	};
  };

  fileSystems."/" = {
    options = [
      "noatime"
      "nodiratime"
      "discard"
    ];
  };

  fileSystems."/" =
    { device = "/dev/mapper/vg_nix-lv_root";
      fsType = "ext4";
    };

  fileSystems."/boot" =
    { device = "/dev/disk/by-uuid/53BC-646A";
      fsType = "vfat";
      options = [ "fmask=0022" "dmask=0022" ];
    };

  swapDevices = [{
  	device = "/var/lib/swapfile";
	size = 4*1024;
  }];

  nixpkgs.hostPlatform = lib.mkDefault "${vars.system}";
}

