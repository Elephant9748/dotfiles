# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=,preferred,auto,auto
monitor=eDP-1,disable
#monitor=eDP-1,preferred, auto, auto
#monitor=eDP-1,preferred, 1920x0, 1
monitor=HDMI-A-1,1920x1080@60,0x0,1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 
exec-once = hyprpaper & waybar & /home/rigel/utility/xdg-portall-hyprland-nuclear & 
exec-once = hyprctl setcursor "Pop" 14 & alacritty

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf


# Some default env vars.
#env = GTK_THEME, Adwaita:dark
env = GDK_BACKEND, wayland,x11
env = QT_QPA_PLATFORMTHEME, qt5ct
#env = GBM_BACKEND, nvidia-drm
#env = __GLX_VENDOR_LIBRARY_NAME, nvidia

# cursor
#env = HYPRCURSOR_THEME,phinger-cursors-dark
env = HYPRCURSOR_THEME,Pop
env = HYPRCURSOR_SIZE,14

#Window-Rules
windowrulev2 = workspace 1, class:^(firefox-aurora)$
windowrulev2 = workspace 1, class:^(steam)$
windowrulev2 = workspace 2, class:^(Alacritty)$
windowrulev2 = workspace 3, class:^(Mullvad Browser Alpha)$
windowrulev2 = workspace 3, class:^(Chromium-browser)$
windowrulev2 = workspace 4, class:^(virt-manager)$
windowrulev2 = workspace 4, class:^(org.gnome.Nautilus)$
windowrulev2 = workspace 4, class:^(Tor Browser)$
windowrulev2 = workspace 4, class:^(Signal)$
#windowrulev2 = workspace 4, class:^(Session)$

#layerrule = noanim, selection

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    gaps_in = 1
    gaps_out = 5
    border_size = 1
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg # Default Color
    col.active_border = rgba(356F89ee) rgba(519C62ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    rounding = 1
    
    blur {
        enabled = true
        size = 3
        passes = 1
    }
    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }
}

misc {
        disable_hyprland_logo = true
        disable_splash_rendering = true
        mouse_move_enables_dpms = true
        key_press_enables_dpms = true
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
    
    # noanim
    # animation = global, 1, 3, default
    # animation = workspaces, 0, 3, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
    smart_resizing = yes
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    # new_is_master = false
    new_status = slave
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
        name = epic-mouse-v1
        sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

#switches
#bindl =,switch:[switcher],exec, swaylock
#bindl =,switch:on:[switcher], exec, hyprctl keyword monitor "HDMI-A-1, 1920x1080@60, 0x0, 1"
#bindl =,switch:off:[switcher], exec, hyprctl keyword monitor "eDP-1, preferred, auto, auto"

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, T, exec, alacritty
bind = $mainMod SHIFT, C, killactive, 

#default exit
bind = $mainMod SHIFT, Q, exit, 

#random hang on exit fix
#bind = $mainMod SHIFT ,Q, exec, /home/rigel/project/dotfiles/scripts/arch/forced_exit_hyprland

bind = SHIFT, R, exec, hyprctl reload,
bind = $mainMod, E, exec, nautilus
bind = $mainMod SHIFT, S, togglefloating, 
#bind = $mainMod SHIFT, P, exec, wofi --show drun
bind = $mainMod, P, exec, rofi -opacity 80 -show drun
bind = $mainMod, C, exec, rofi -show calc -modi calc -no-show-match -no-sort -no-history -calc-command "echo '{result}' | wl-copy"
bind = $mainMod SHIFT, P, exec, rofi -show power-menu -modi "power-menu:rofi-power-menu --choices=shutdown/lockscreen/logout/reboot"
bind = $mainMod SHIFT, D, pseudo, # dwindle
bind = $mainMod SHIFT, J, togglesplit, # dwindle
bind = $mainMod SHIFT, O, exec, hyprpicker -a --format=hex # hyprpicker

# Clear clipboard binds
bind = $mainMod, W, exec, wl-copy -c

# Screenshot binds
bind = $mainMod SHIFT, X, exec, grim -g "$(slurp)" - | swappy -f -

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# wallpaper set to workspace
# $w1 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/a.png" # Not set a.png
# $w1 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/i.png"
$w2 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/ii.png"
$w3 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/iii.png"
$w4 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/iv.png"
$w5 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/v.png"
$w6 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/vi.png"
$w7 = hyprctl hyprpaper wallpaper "HDMI-A-1, ~/Pictures/wallhaven.cc/vii.png"

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
# bind = $mainMod, 1, exec,$w1 # hyprpaper set wallpaper
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 2, exec,$w2 # hyprpaper set wallpaper
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 3, exec,$w3 # hyprpaper set wallpaper
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 4, exec,$w4 # hyprpaper set wallpaper
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 5, exec,$w5 # hyprpaper set wallpaper
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 6, exec,$w6 # hyprpaper set wallpaper
bind = $mainMod, 7, workspace, 7
# bind = $mainMod, 7, exec,$w7 # hyprpaper set wallpaper
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
# bind = $mainMod SHIFT, 1, exec,$w1 # hyprpaper set wallpaper
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 2, exec,$w2 # hyprpaper set wallpaper
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 3, exec,$w3 # hyprpaper set wallpaper
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 4, exec,$w4 # hyprpaper set wallpaper
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 5, exec,$w5 # hyprpaper set wallpaper
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 6, exec,$w6 # hyprpaper set wallpaper
bind = $mainMod SHIFT, 7, movetoworkspace, 7
# bind = $mainMod SHIFT, 7, exec,$w7 # hyprpaper set wallpaper
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
